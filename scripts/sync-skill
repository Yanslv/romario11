#!/bin/sh
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

case "$(uname -s)" in
  MINGW*|MSYS*|CYGWIN*)
    if command -v powershell.exe >/dev/null 2>&1; then
      powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$SCRIPT_DIR/sync-skill.ps1"
    else
      sh "$SCRIPT_DIR/sync-skill.sh"
    fi
    ;;
  *)
    sh "$SCRIPT_DIR/sync-skill.sh"
    ;;
esac
